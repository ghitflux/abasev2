/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { AssociadoMeRetrieveQueryResponse } from "../models/AssociadoMeRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { associadoMeRetrieve } from "../client/associadoMeRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const associadoMeRetrieveQueryKey = () =>
  [{ url: "/api/associado/me" }] as const;

export type AssociadoMeRetrieveQueryKey = ReturnType<
  typeof associadoMeRetrieveQueryKey
>;

export function associadoMeRetrieveQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = associadoMeRetrieveQueryKey();
  return queryOptions<
    AssociadoMeRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    AssociadoMeRetrieveQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return associadoMeRetrieve({
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/associado/me}
 */
export function useAssociadoMeRetrieve<
  TData = AssociadoMeRetrieveQueryResponse,
  TQueryData = AssociadoMeRetrieveQueryResponse,
  TQueryKey extends QueryKey = AssociadoMeRetrieveQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        AssociadoMeRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? associadoMeRetrieveQueryKey();

  const query = useQuery(
    {
      ...associadoMeRetrieveQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
