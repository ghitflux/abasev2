/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1RenovacaoCiclosListQueryResponse,
  V1RenovacaoCiclosListQueryParams,
} from "../models/V1RenovacaoCiclosList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1RenovacaoCiclosList } from "../client/v1RenovacaoCiclosList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1RenovacaoCiclosListQueryKey = (
  params?: V1RenovacaoCiclosListQueryParams,
) =>
  [{ url: "/api/v1/renovacao-ciclos/" }, ...(params ? [params] : [])] as const;

export type V1RenovacaoCiclosListQueryKey = ReturnType<
  typeof v1RenovacaoCiclosListQueryKey
>;

export function v1RenovacaoCiclosListQueryOptions(
  params?: V1RenovacaoCiclosListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1RenovacaoCiclosListQueryKey(params);
  return queryOptions<
    V1RenovacaoCiclosListQueryResponse,
    ResponseErrorConfig<Error>,
    V1RenovacaoCiclosListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1RenovacaoCiclosList(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/renovacao-ciclos/}
 */
export function useV1RenovacaoCiclosList<
  TData = V1RenovacaoCiclosListQueryResponse,
  TQueryData = V1RenovacaoCiclosListQueryResponse,
  TQueryKey extends QueryKey = V1RenovacaoCiclosListQueryKey,
>(
  params?: V1RenovacaoCiclosListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1RenovacaoCiclosListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? v1RenovacaoCiclosListQueryKey(params);

  const query = useQuery(
    {
      ...v1RenovacaoCiclosListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
