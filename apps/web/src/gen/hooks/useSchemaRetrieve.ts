/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  SchemaRetrieveQueryResponse,
  SchemaRetrieveQueryParams,
} from "../models/SchemaRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { schemaRetrieve } from "../client/schemaRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const schemaRetrieveQueryKey = (params?: SchemaRetrieveQueryParams) =>
  [{ url: "/api/schema/" }, ...(params ? [params] : [])] as const;

export type SchemaRetrieveQueryKey = ReturnType<typeof schemaRetrieveQueryKey>;

export function schemaRetrieveQueryOptions(
  params?: SchemaRetrieveQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = schemaRetrieveQueryKey(params);
  return queryOptions<
    SchemaRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    SchemaRetrieveQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return schemaRetrieve(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @description OpenApi3 schema for this API. Format can be selected via content negotiation.
 * - YAML: application/vnd.oai.openapi
 * - JSON: application/vnd.oai.openapi+json
 * {@link /api/schema/}
 */
export function useSchemaRetrieve<
  TData = SchemaRetrieveQueryResponse,
  TQueryData = SchemaRetrieveQueryResponse,
  TQueryKey extends QueryKey = SchemaRetrieveQueryKey,
>(
  params?: SchemaRetrieveQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        SchemaRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? schemaRetrieveQueryKey(params);

  const query = useQuery(
    {
      ...schemaRetrieveQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
