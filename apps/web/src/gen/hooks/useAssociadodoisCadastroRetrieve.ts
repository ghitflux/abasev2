/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { AssociadodoisCadastroRetrieveQueryResponse } from "../models/AssociadodoisCadastroRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { associadodoisCadastroRetrieve } from "../client/associadodoisCadastroRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const associadodoisCadastroRetrieveQueryKey = () =>
  [{ url: "/api/associadodois/cadastro" }] as const;

export type AssociadodoisCadastroRetrieveQueryKey = ReturnType<
  typeof associadodoisCadastroRetrieveQueryKey
>;

export function associadodoisCadastroRetrieveQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = associadodoisCadastroRetrieveQueryKey();
  return queryOptions<
    AssociadodoisCadastroRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    AssociadodoisCadastroRetrieveQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return associadodoisCadastroRetrieve({
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/associadodois/cadastro}
 */
export function useAssociadodoisCadastroRetrieve<
  TData = AssociadodoisCadastroRetrieveQueryResponse,
  TQueryData = AssociadodoisCadastroRetrieveQueryResponse,
  TQueryKey extends QueryKey = AssociadodoisCadastroRetrieveQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        AssociadodoisCadastroRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? associadodoisCadastroRetrieveQueryKey();

  const query = useQuery(
    {
      ...associadodoisCadastroRetrieveQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
