/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1CoordenacaoRefinanciamentoListQueryResponse,
  V1CoordenacaoRefinanciamentoListQueryParams,
} from "../models/V1CoordenacaoRefinanciamentoList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1CoordenacaoRefinanciamentoList } from "../client/v1CoordenacaoRefinanciamentoList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1CoordenacaoRefinanciamentoListQueryKey = (
  params?: V1CoordenacaoRefinanciamentoListQueryParams,
) =>
  [
    { url: "/api/v1/coordenacao/refinanciamento/" },
    ...(params ? [params] : []),
  ] as const;

export type V1CoordenacaoRefinanciamentoListQueryKey = ReturnType<
  typeof v1CoordenacaoRefinanciamentoListQueryKey
>;

export function v1CoordenacaoRefinanciamentoListQueryOptions(
  params?: V1CoordenacaoRefinanciamentoListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1CoordenacaoRefinanciamentoListQueryKey(params);
  return queryOptions<
    V1CoordenacaoRefinanciamentoListQueryResponse,
    ResponseErrorConfig<Error>,
    V1CoordenacaoRefinanciamentoListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1CoordenacaoRefinanciamentoList(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/coordenacao/refinanciamento/}
 */
export function useV1CoordenacaoRefinanciamentoList<
  TData = V1CoordenacaoRefinanciamentoListQueryResponse,
  TQueryData = V1CoordenacaoRefinanciamentoListQueryResponse,
  TQueryKey extends QueryKey = V1CoordenacaoRefinanciamentoListQueryKey,
>(
  params?: V1CoordenacaoRefinanciamentoListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1CoordenacaoRefinanciamentoListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ??
    v1CoordenacaoRefinanciamentoListQueryKey(params);

  const query = useQuery(
    {
      ...v1CoordenacaoRefinanciamentoListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
