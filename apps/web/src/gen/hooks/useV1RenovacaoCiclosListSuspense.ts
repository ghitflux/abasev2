/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1RenovacaoCiclosListQueryResponse,
  V1RenovacaoCiclosListQueryParams,
} from "../models/V1RenovacaoCiclosList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { v1RenovacaoCiclosList } from "../client/v1RenovacaoCiclosList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const v1RenovacaoCiclosListSuspenseQueryKey = (
  params?: V1RenovacaoCiclosListQueryParams,
) =>
  [{ url: "/api/v1/renovacao-ciclos/" }, ...(params ? [params] : [])] as const;

export type V1RenovacaoCiclosListSuspenseQueryKey = ReturnType<
  typeof v1RenovacaoCiclosListSuspenseQueryKey
>;

export function v1RenovacaoCiclosListSuspenseQueryOptions(
  params?: V1RenovacaoCiclosListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1RenovacaoCiclosListSuspenseQueryKey(params);
  return queryOptions<
    V1RenovacaoCiclosListQueryResponse,
    ResponseErrorConfig<Error>,
    V1RenovacaoCiclosListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1RenovacaoCiclosList(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/renovacao-ciclos/}
 */
export function useV1RenovacaoCiclosListSuspense<
  TData = V1RenovacaoCiclosListQueryResponse,
  TQueryKey extends QueryKey = V1RenovacaoCiclosListSuspenseQueryKey,
>(
  params?: V1RenovacaoCiclosListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        V1RenovacaoCiclosListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? v1RenovacaoCiclosListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...v1RenovacaoCiclosListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
