/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { AssociadodoisStatusRetrieveQueryResponse } from "../models/AssociadodoisStatusRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { associadodoisStatusRetrieve } from "../client/associadodoisStatusRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const associadodoisStatusRetrieveQueryKey = () =>
  [{ url: "/api/associadodois/status" }] as const;

export type AssociadodoisStatusRetrieveQueryKey = ReturnType<
  typeof associadodoisStatusRetrieveQueryKey
>;

export function associadodoisStatusRetrieveQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = associadodoisStatusRetrieveQueryKey();
  return queryOptions<
    AssociadodoisStatusRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    AssociadodoisStatusRetrieveQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return associadodoisStatusRetrieve({
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/associadodois/status}
 */
export function useAssociadodoisStatusRetrieve<
  TData = AssociadodoisStatusRetrieveQueryResponse,
  TQueryData = AssociadodoisStatusRetrieveQueryResponse,
  TQueryKey extends QueryKey = AssociadodoisStatusRetrieveQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        AssociadodoisStatusRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? associadodoisStatusRetrieveQueryKey();

  const query = useQuery(
    {
      ...associadodoisStatusRetrieveQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
