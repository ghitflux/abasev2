/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1AgentePagamentosListQueryResponse,
  V1AgentePagamentosListQueryParams,
} from "../models/V1AgentePagamentosList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { v1AgentePagamentosList } from "../client/v1AgentePagamentosList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const v1AgentePagamentosListSuspenseQueryKey = (
  params?: V1AgentePagamentosListQueryParams,
) =>
  [{ url: "/api/v1/agente/pagamentos/" }, ...(params ? [params] : [])] as const;

export type V1AgentePagamentosListSuspenseQueryKey = ReturnType<
  typeof v1AgentePagamentosListSuspenseQueryKey
>;

export function v1AgentePagamentosListSuspenseQueryOptions(
  params?: V1AgentePagamentosListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1AgentePagamentosListSuspenseQueryKey(params);
  return queryOptions<
    V1AgentePagamentosListQueryResponse,
    ResponseErrorConfig<Error>,
    V1AgentePagamentosListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1AgentePagamentosList(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/agente/pagamentos/}
 */
export function useV1AgentePagamentosListSuspense<
  TData = V1AgentePagamentosListQueryResponse,
  TQueryKey extends QueryKey = V1AgentePagamentosListSuspenseQueryKey,
>(
  params?: V1AgentePagamentosListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        V1AgentePagamentosListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? v1AgentePagamentosListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...v1AgentePagamentosListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
