/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { AuthCheckEmailRetrieveQueryResponse } from "../models/AuthCheckEmailRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { authCheckEmailRetrieve } from "../client/authCheckEmailRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const authCheckEmailRetrieveQueryKey = () =>
  [{ url: "/api/auth/check-email" }] as const;

export type AuthCheckEmailRetrieveQueryKey = ReturnType<
  typeof authCheckEmailRetrieveQueryKey
>;

export function authCheckEmailRetrieveQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = authCheckEmailRetrieveQueryKey();
  return queryOptions<
    AuthCheckEmailRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    AuthCheckEmailRetrieveQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return authCheckEmailRetrieve({
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/auth/check-email}
 */
export function useAuthCheckEmailRetrieve<
  TData = AuthCheckEmailRetrieveQueryResponse,
  TQueryData = AuthCheckEmailRetrieveQueryResponse,
  TQueryKey extends QueryKey = AuthCheckEmailRetrieveQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        AuthCheckEmailRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? authCheckEmailRetrieveQueryKey();

  const query = useQuery(
    {
      ...authCheckEmailRetrieveQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
