/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1AgentePagamentosListQueryResponse,
  V1AgentePagamentosListQueryParams,
} from "../models/V1AgentePagamentosList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1AgentePagamentosList } from "../client/v1AgentePagamentosList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1AgentePagamentosListQueryKey = (
  params?: V1AgentePagamentosListQueryParams,
) =>
  [{ url: "/api/v1/agente/pagamentos/" }, ...(params ? [params] : [])] as const;

export type V1AgentePagamentosListQueryKey = ReturnType<
  typeof v1AgentePagamentosListQueryKey
>;

export function v1AgentePagamentosListQueryOptions(
  params?: V1AgentePagamentosListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1AgentePagamentosListQueryKey(params);
  return queryOptions<
    V1AgentePagamentosListQueryResponse,
    ResponseErrorConfig<Error>,
    V1AgentePagamentosListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1AgentePagamentosList(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/agente/pagamentos/}
 */
export function useV1AgentePagamentosList<
  TData = V1AgentePagamentosListQueryResponse,
  TQueryData = V1AgentePagamentosListQueryResponse,
  TQueryKey extends QueryKey = V1AgentePagamentosListQueryKey,
>(
  params?: V1AgentePagamentosListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1AgentePagamentosListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? v1AgentePagamentosListQueryKey(params);

  const query = useQuery(
    {
      ...v1AgentePagamentosListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
