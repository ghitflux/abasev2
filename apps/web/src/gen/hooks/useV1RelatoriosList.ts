/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1RelatoriosListQueryResponse,
  V1RelatoriosListQueryParams,
} from "../models/V1RelatoriosList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1RelatoriosList } from "../client/v1RelatoriosList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1RelatoriosListQueryKey = (
  params?: V1RelatoriosListQueryParams,
) => [{ url: "/api/v1/relatorios/" }, ...(params ? [params] : [])] as const;

export type V1RelatoriosListQueryKey = ReturnType<
  typeof v1RelatoriosListQueryKey
>;

export function v1RelatoriosListQueryOptions(
  params?: V1RelatoriosListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1RelatoriosListQueryKey(params);
  return queryOptions<
    V1RelatoriosListQueryResponse,
    ResponseErrorConfig<Error>,
    V1RelatoriosListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1RelatoriosList(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/relatorios/}
 */
export function useV1RelatoriosList<
  TData = V1RelatoriosListQueryResponse,
  TQueryData = V1RelatoriosListQueryResponse,
  TQueryKey extends QueryKey = V1RelatoriosListQueryKey,
>(
  params?: V1RelatoriosListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1RelatoriosListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? v1RelatoriosListQueryKey(params);

  const query = useQuery(
    {
      ...v1RelatoriosListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
