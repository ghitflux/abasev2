/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1AnaliseListQueryResponse,
  V1AnaliseListQueryParams,
} from "../models/V1AnaliseList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1AnaliseList } from "../client/v1AnaliseList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1AnaliseListQueryKey = (params?: V1AnaliseListQueryParams) =>
  [{ url: "/api/v1/analise/" }, ...(params ? [params] : [])] as const;

export type V1AnaliseListQueryKey = ReturnType<typeof v1AnaliseListQueryKey>;

export function v1AnaliseListQueryOptions(
  params?: V1AnaliseListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1AnaliseListQueryKey(params);
  return queryOptions<
    V1AnaliseListQueryResponse,
    ResponseErrorConfig<Error>,
    V1AnaliseListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1AnaliseList(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/analise/}
 */
export function useV1AnaliseList<
  TData = V1AnaliseListQueryResponse,
  TQueryData = V1AnaliseListQueryResponse,
  TQueryKey extends QueryKey = V1AnaliseListQueryKey,
>(
  params?: V1AnaliseListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1AnaliseListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? v1AnaliseListQueryKey(params);

  const query = useQuery(
    {
      ...v1AnaliseListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
