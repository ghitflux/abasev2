/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1RenovacaoCiclosExportarRetrieveQueryResponse,
  V1RenovacaoCiclosExportarRetrieveQueryParams,
} from "../models/V1RenovacaoCiclosExportarRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1RenovacaoCiclosExportarRetrieve } from "../client/v1RenovacaoCiclosExportarRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1RenovacaoCiclosExportarRetrieveQueryKey = (
  params?: V1RenovacaoCiclosExportarRetrieveQueryParams,
) =>
  [
    { url: "/api/v1/renovacao-ciclos/exportar/" },
    ...(params ? [params] : []),
  ] as const;

export type V1RenovacaoCiclosExportarRetrieveQueryKey = ReturnType<
  typeof v1RenovacaoCiclosExportarRetrieveQueryKey
>;

export function v1RenovacaoCiclosExportarRetrieveQueryOptions(
  params?: V1RenovacaoCiclosExportarRetrieveQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1RenovacaoCiclosExportarRetrieveQueryKey(params);
  return queryOptions<
    V1RenovacaoCiclosExportarRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    V1RenovacaoCiclosExportarRetrieveQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1RenovacaoCiclosExportarRetrieve(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/renovacao-ciclos/exportar/}
 */
export function useV1RenovacaoCiclosExportarRetrieve<
  TData = V1RenovacaoCiclosExportarRetrieveQueryResponse,
  TQueryData = V1RenovacaoCiclosExportarRetrieveQueryResponse,
  TQueryKey extends QueryKey = V1RenovacaoCiclosExportarRetrieveQueryKey,
>(
  params?: V1RenovacaoCiclosExportarRetrieveQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1RenovacaoCiclosExportarRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ??
    v1RenovacaoCiclosExportarRetrieveQueryKey(params);

  const query = useQuery(
    {
      ...v1RenovacaoCiclosExportarRetrieveQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
