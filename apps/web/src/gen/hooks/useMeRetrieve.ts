/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { MeRetrieveQueryResponse } from "../models/MeRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { meRetrieve } from "../client/meRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const meRetrieveQueryKey = () => [{ url: "/api/me" }] as const;

export type MeRetrieveQueryKey = ReturnType<typeof meRetrieveQueryKey>;

export function meRetrieveQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = meRetrieveQueryKey();
  return queryOptions<
    MeRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    MeRetrieveQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return meRetrieve({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * {@link /api/me}
 */
export function useMeRetrieve<
  TData = MeRetrieveQueryResponse,
  TQueryData = MeRetrieveQueryResponse,
  TQueryKey extends QueryKey = MeRetrieveQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        MeRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? meRetrieveQueryKey();

  const query = useQuery(
    {
      ...meRetrieveQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
