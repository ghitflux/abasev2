/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1RenovacaoCiclosVisaoMensalRetrieveQueryResponse,
  V1RenovacaoCiclosVisaoMensalRetrieveQueryParams,
} from "../models/V1RenovacaoCiclosVisaoMensalRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { v1RenovacaoCiclosVisaoMensalRetrieve } from "../client/v1RenovacaoCiclosVisaoMensalRetrieve.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const v1RenovacaoCiclosVisaoMensalRetrieveSuspenseQueryKey = (
  params?: V1RenovacaoCiclosVisaoMensalRetrieveQueryParams,
) =>
  [
    { url: "/api/v1/renovacao-ciclos/visao-mensal/" },
    ...(params ? [params] : []),
  ] as const;

export type V1RenovacaoCiclosVisaoMensalRetrieveSuspenseQueryKey = ReturnType<
  typeof v1RenovacaoCiclosVisaoMensalRetrieveSuspenseQueryKey
>;

export function v1RenovacaoCiclosVisaoMensalRetrieveSuspenseQueryOptions(
  params?: V1RenovacaoCiclosVisaoMensalRetrieveQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1RenovacaoCiclosVisaoMensalRetrieveSuspenseQueryKey(params);
  return queryOptions<
    V1RenovacaoCiclosVisaoMensalRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    V1RenovacaoCiclosVisaoMensalRetrieveQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1RenovacaoCiclosVisaoMensalRetrieve(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/renovacao-ciclos/visao-mensal/}
 */
export function useV1RenovacaoCiclosVisaoMensalRetrieveSuspense<
  TData = V1RenovacaoCiclosVisaoMensalRetrieveQueryResponse,
  TQueryKey extends QueryKey =
    V1RenovacaoCiclosVisaoMensalRetrieveSuspenseQueryKey,
>(
  params?: V1RenovacaoCiclosVisaoMensalRetrieveQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        V1RenovacaoCiclosVisaoMensalRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ??
    v1RenovacaoCiclosVisaoMensalRetrieveSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...v1RenovacaoCiclosVisaoMensalRetrieveSuspenseQueryOptions(
        params,
        config,
      ),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
