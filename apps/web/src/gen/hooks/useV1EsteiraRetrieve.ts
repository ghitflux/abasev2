/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1EsteiraRetrieveQueryResponse,
  V1EsteiraRetrievePathParams,
} from "../models/V1EsteiraRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1EsteiraRetrieve } from "../client/v1EsteiraRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1EsteiraRetrieveQueryKey = (
  id: V1EsteiraRetrievePathParams["id"],
) => [{ url: "/api/v1/esteira/:id/", params: { id: id } }] as const;

export type V1EsteiraRetrieveQueryKey = ReturnType<
  typeof v1EsteiraRetrieveQueryKey
>;

export function v1EsteiraRetrieveQueryOptions(
  id: V1EsteiraRetrievePathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1EsteiraRetrieveQueryKey(id);
  return queryOptions<
    V1EsteiraRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    V1EsteiraRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return v1EsteiraRetrieve(id, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/esteira/:id/}
 */
export function useV1EsteiraRetrieve<
  TData = V1EsteiraRetrieveQueryResponse,
  TQueryData = V1EsteiraRetrieveQueryResponse,
  TQueryKey extends QueryKey = V1EsteiraRetrieveQueryKey,
>(
  id: V1EsteiraRetrievePathParams["id"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1EsteiraRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? v1EsteiraRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...v1EsteiraRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
