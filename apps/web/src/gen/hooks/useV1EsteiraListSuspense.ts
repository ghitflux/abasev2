/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1EsteiraListQueryResponse,
  V1EsteiraListQueryParams,
} from "../models/V1EsteiraList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { v1EsteiraList } from "../client/v1EsteiraList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const v1EsteiraListSuspenseQueryKey = (
  params?: V1EsteiraListQueryParams,
) => [{ url: "/api/v1/esteira/" }, ...(params ? [params] : [])] as const;

export type V1EsteiraListSuspenseQueryKey = ReturnType<
  typeof v1EsteiraListSuspenseQueryKey
>;

export function v1EsteiraListSuspenseQueryOptions(
  params?: V1EsteiraListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1EsteiraListSuspenseQueryKey(params);
  return queryOptions<
    V1EsteiraListQueryResponse,
    ResponseErrorConfig<Error>,
    V1EsteiraListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1EsteiraList(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/esteira/}
 */
export function useV1EsteiraListSuspense<
  TData = V1EsteiraListQueryResponse,
  TQueryKey extends QueryKey = V1EsteiraListSuspenseQueryKey,
>(
  params?: V1EsteiraListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        V1EsteiraListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? v1EsteiraListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...v1EsteiraListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
