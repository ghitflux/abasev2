/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1RefinanciamentosListQueryResponse,
  V1RefinanciamentosListQueryParams,
} from "../models/V1RefinanciamentosList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1RefinanciamentosList } from "../client/v1RefinanciamentosList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1RefinanciamentosListQueryKey = (
  params?: V1RefinanciamentosListQueryParams,
) =>
  [{ url: "/api/v1/refinanciamentos/" }, ...(params ? [params] : [])] as const;

export type V1RefinanciamentosListQueryKey = ReturnType<
  typeof v1RefinanciamentosListQueryKey
>;

export function v1RefinanciamentosListQueryOptions(
  params?: V1RefinanciamentosListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1RefinanciamentosListQueryKey(params);
  return queryOptions<
    V1RefinanciamentosListQueryResponse,
    ResponseErrorConfig<Error>,
    V1RefinanciamentosListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1RefinanciamentosList(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/refinanciamentos/}
 */
export function useV1RefinanciamentosList<
  TData = V1RefinanciamentosListQueryResponse,
  TQueryData = V1RefinanciamentosListQueryResponse,
  TQueryKey extends QueryKey = V1RefinanciamentosListQueryKey,
>(
  params?: V1RefinanciamentosListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1RefinanciamentosListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? v1RefinanciamentosListQueryKey(params);

  const query = useQuery(
    {
      ...v1RefinanciamentosListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
