/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1ContratosListQueryResponse,
  V1ContratosListQueryParams,
} from "../models/V1ContratosList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1ContratosList } from "../client/v1ContratosList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1ContratosListQueryKey = (params?: V1ContratosListQueryParams) =>
  [{ url: "/api/v1/contratos/" }, ...(params ? [params] : [])] as const;

export type V1ContratosListQueryKey = ReturnType<
  typeof v1ContratosListQueryKey
>;

export function v1ContratosListQueryOptions(
  params?: V1ContratosListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1ContratosListQueryKey(params);
  return queryOptions<
    V1ContratosListQueryResponse,
    ResponseErrorConfig<Error>,
    V1ContratosListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1ContratosList(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/contratos/}
 */
export function useV1ContratosList<
  TData = V1ContratosListQueryResponse,
  TQueryData = V1ContratosListQueryResponse,
  TQueryKey extends QueryKey = V1ContratosListQueryKey,
>(
  params?: V1ContratosListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1ContratosListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? v1ContratosListQueryKey(params);

  const query = useQuery(
    {
      ...v1ContratosListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
