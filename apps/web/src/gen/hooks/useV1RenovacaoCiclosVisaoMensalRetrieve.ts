/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1RenovacaoCiclosVisaoMensalRetrieveQueryResponse,
  V1RenovacaoCiclosVisaoMensalRetrieveQueryParams,
} from "../models/V1RenovacaoCiclosVisaoMensalRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1RenovacaoCiclosVisaoMensalRetrieve } from "../client/v1RenovacaoCiclosVisaoMensalRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1RenovacaoCiclosVisaoMensalRetrieveQueryKey = (
  params?: V1RenovacaoCiclosVisaoMensalRetrieveQueryParams,
) =>
  [
    { url: "/api/v1/renovacao-ciclos/visao-mensal/" },
    ...(params ? [params] : []),
  ] as const;

export type V1RenovacaoCiclosVisaoMensalRetrieveQueryKey = ReturnType<
  typeof v1RenovacaoCiclosVisaoMensalRetrieveQueryKey
>;

export function v1RenovacaoCiclosVisaoMensalRetrieveQueryOptions(
  params?: V1RenovacaoCiclosVisaoMensalRetrieveQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1RenovacaoCiclosVisaoMensalRetrieveQueryKey(params);
  return queryOptions<
    V1RenovacaoCiclosVisaoMensalRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    V1RenovacaoCiclosVisaoMensalRetrieveQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1RenovacaoCiclosVisaoMensalRetrieve(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/renovacao-ciclos/visao-mensal/}
 */
export function useV1RenovacaoCiclosVisaoMensalRetrieve<
  TData = V1RenovacaoCiclosVisaoMensalRetrieveQueryResponse,
  TQueryData = V1RenovacaoCiclosVisaoMensalRetrieveQueryResponse,
  TQueryKey extends QueryKey = V1RenovacaoCiclosVisaoMensalRetrieveQueryKey,
>(
  params?: V1RenovacaoCiclosVisaoMensalRetrieveQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1RenovacaoCiclosVisaoMensalRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ??
    v1RenovacaoCiclosVisaoMensalRetrieveQueryKey(params);

  const query = useQuery(
    {
      ...v1RenovacaoCiclosVisaoMensalRetrieveQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
