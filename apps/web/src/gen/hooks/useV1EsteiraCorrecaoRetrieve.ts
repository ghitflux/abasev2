/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1EsteiraCorrecaoRetrieveQueryResponse,
  V1EsteiraCorrecaoRetrievePathParams,
} from "../models/V1EsteiraCorrecaoRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1EsteiraCorrecaoRetrieve } from "../client/v1EsteiraCorrecaoRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1EsteiraCorrecaoRetrieveQueryKey = (
  id: V1EsteiraCorrecaoRetrievePathParams["id"],
) => [{ url: "/api/v1/esteira/:id/correcao/", params: { id: id } }] as const;

export type V1EsteiraCorrecaoRetrieveQueryKey = ReturnType<
  typeof v1EsteiraCorrecaoRetrieveQueryKey
>;

export function v1EsteiraCorrecaoRetrieveQueryOptions(
  id: V1EsteiraCorrecaoRetrievePathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1EsteiraCorrecaoRetrieveQueryKey(id);
  return queryOptions<
    V1EsteiraCorrecaoRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    V1EsteiraCorrecaoRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return v1EsteiraCorrecaoRetrieve(id, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/esteira/:id/correcao/}
 */
export function useV1EsteiraCorrecaoRetrieve<
  TData = V1EsteiraCorrecaoRetrieveQueryResponse,
  TQueryData = V1EsteiraCorrecaoRetrieveQueryResponse,
  TQueryKey extends QueryKey = V1EsteiraCorrecaoRetrieveQueryKey,
>(
  id: V1EsteiraCorrecaoRetrievePathParams["id"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1EsteiraCorrecaoRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? v1EsteiraCorrecaoRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...v1EsteiraCorrecaoRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
