/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { V1AuthMeRetrieveQueryResponse } from "../models/V1AuthMeRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1AuthMeRetrieve } from "../client/v1AuthMeRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1AuthMeRetrieveQueryKey = () =>
  [{ url: "/api/v1/auth/me/" }] as const;

export type V1AuthMeRetrieveQueryKey = ReturnType<
  typeof v1AuthMeRetrieveQueryKey
>;

export function v1AuthMeRetrieveQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1AuthMeRetrieveQueryKey();
  return queryOptions<
    V1AuthMeRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    V1AuthMeRetrieveQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1AuthMeRetrieve({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * {@link /api/v1/auth/me/}
 */
export function useV1AuthMeRetrieve<
  TData = V1AuthMeRetrieveQueryResponse,
  TQueryData = V1AuthMeRetrieveQueryResponse,
  TQueryKey extends QueryKey = V1AuthMeRetrieveQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1AuthMeRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? v1AuthMeRetrieveQueryKey();

  const query = useQuery(
    {
      ...v1AuthMeRetrieveQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
