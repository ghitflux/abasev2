/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  V1EsteiraListQueryResponse,
  V1EsteiraListQueryParams,
} from "../models/V1EsteiraList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { v1EsteiraList } from "../client/v1EsteiraList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const v1EsteiraListQueryKey = (params?: V1EsteiraListQueryParams) =>
  [{ url: "/api/v1/esteira/" }, ...(params ? [params] : [])] as const;

export type V1EsteiraListQueryKey = ReturnType<typeof v1EsteiraListQueryKey>;

export function v1EsteiraListQueryOptions(
  params?: V1EsteiraListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = v1EsteiraListQueryKey(params);
  return queryOptions<
    V1EsteiraListQueryResponse,
    ResponseErrorConfig<Error>,
    V1EsteiraListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return v1EsteiraList(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/v1/esteira/}
 */
export function useV1EsteiraList<
  TData = V1EsteiraListQueryResponse,
  TQueryData = V1EsteiraListQueryResponse,
  TQueryKey extends QueryKey = V1EsteiraListQueryKey,
>(
  params?: V1EsteiraListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        V1EsteiraListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? v1EsteiraListQueryKey(params);

  const query = useQuery(
    {
      ...v1EsteiraListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
