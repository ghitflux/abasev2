/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { HomeRetrieveQueryResponse } from "../models/HomeRetrieve.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { homeRetrieve } from "../client/homeRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const homeRetrieveQueryKey = () => [{ url: "/api/home" }] as const;

export type HomeRetrieveQueryKey = ReturnType<typeof homeRetrieveQueryKey>;

export function homeRetrieveQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = homeRetrieveQueryKey();
  return queryOptions<
    HomeRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    HomeRetrieveQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return homeRetrieve({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * {@link /api/home}
 */
export function useHomeRetrieve<
  TData = HomeRetrieveQueryResponse,
  TQueryData = HomeRetrieveQueryResponse,
  TQueryKey extends QueryKey = HomeRetrieveQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        HomeRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? homeRetrieveQueryKey();

  const query = useQuery(
    {
      ...homeRetrieveQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
