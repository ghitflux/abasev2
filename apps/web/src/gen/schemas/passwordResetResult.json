{
  "type": "object",
  "properties": {
    "temporary_password": { "type": "string" },
    "must_set_password": { "type": "boolean" }
  },
  "required": ["must_set_password", "temporary_password"],
  "x-readme-ref-name": "PasswordResetResult"
}
