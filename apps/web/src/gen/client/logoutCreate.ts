/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { LogoutCreateMutationResponse } from "../models/LogoutCreate.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getLogoutCreateUrl() {
  const res = { method: "POST", url: `/api/logout` as const };
  return res;
}

/**
 * {@link /api/logout}
 */
export async function logoutCreate(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    LogoutCreateMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getLogoutCreateUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
