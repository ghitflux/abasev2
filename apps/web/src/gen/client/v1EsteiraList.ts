/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  V1EsteiraListQueryResponse,
  V1EsteiraListQueryParams,
} from "../models/V1EsteiraList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getV1EsteiraListUrl() {
  const res = { method: "GET", url: `/api/v1/esteira/` as const };
  return res;
}

/**
 * {@link /api/v1/esteira/}
 */
export async function v1EsteiraList(
  params?: V1EsteiraListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    V1EsteiraListQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getV1EsteiraListUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
