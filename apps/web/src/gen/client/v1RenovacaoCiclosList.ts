/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  V1RenovacaoCiclosListQueryResponse,
  V1RenovacaoCiclosListQueryParams,
} from "../models/V1RenovacaoCiclosList.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getV1RenovacaoCiclosListUrl() {
  const res = { method: "GET", url: `/api/v1/renovacao-ciclos/` as const };
  return res;
}

/**
 * {@link /api/v1/renovacao-ciclos/}
 */
export async function v1RenovacaoCiclosList(
  params?: V1RenovacaoCiclosListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    V1RenovacaoCiclosListQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getV1RenovacaoCiclosListUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
