/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  V1EsteiraDestroyMutationResponse,
  V1EsteiraDestroyPathParams,
} from "../models/V1EsteiraDestroy.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getV1EsteiraDestroyUrl(id: V1EsteiraDestroyPathParams["id"]) {
  const res = { method: "DELETE", url: `/api/v1/esteira/${id}/` as const };
  return res;
}

/**
 * {@link /api/v1/esteira/:id/}
 */
export async function v1EsteiraDestroy(
  id: V1EsteiraDestroyPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    V1EsteiraDestroyMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "DELETE",
    url: getV1EsteiraDestroyUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
