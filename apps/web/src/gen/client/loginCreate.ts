/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { LoginCreateMutationResponse } from "../models/LoginCreate.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getLoginCreateUrl() {
  const res = { method: "POST", url: `/api/login` as const };
  return res;
}

/**
 * {@link /api/login}
 */
export async function loginCreate(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    LoginCreateMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getLoginCreateUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
